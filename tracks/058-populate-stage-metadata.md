# Track 058: Populate Stage Management Metadata

## 1. Plan
- Enhance the metadata for Ignition, Discovery, Branding, and Outreach stages.
- Implement the "Dual-Logic AI" (The Kick vs The Spark) persona across all stages for consistency.
- Define specific objectives and action triggers for each stage.
- Synchronize `src/config/journeyStages.ts` and `src/config/defaultStageContent.ts`.
- Update the Admin Dashboard's default seeding data.

## 2. Setup
- Review existing prompts in `src/config/journeyStages.ts`.
- Align with the "Sparkwavv" brand voice (Radical Transparency, DNA-based, High Performance).

## 3. Build
- [x] Update `src/config/journeyStages.ts` with enhanced prompts.
- [x] Update `src/config/defaultStageContent.ts` to reflect the new defaults.
- [x] Verify linting.
- [x] Update `CHANGELOG.md`.
